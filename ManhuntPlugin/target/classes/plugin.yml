name: ManhuntPlugin
version: '1.0-SNAPSHOT'
main: it.manhuntpl.ManhuntPlugin
api-version: '1.21'

commands:
  manhunt:
    description: Main command Manhunt (help, create, accept, reject, join, allow, leave, compass)
    usage: /manhunt <help|create|accept|reject|join|allow|leave|compass follow> [...]
    aliases: [mh]
